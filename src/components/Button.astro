---
export interface Props {
  href?: string;
}

const { href, ...props } = Astro.props;
---

{href ? (
  <a href={href} class="px-2 py-1 rounded-[1px] border-black border block hover:bg-gray-100 transition-colors" {...props}>
    <slot />
  </a>
) : (
  <button class="px-2 py-1 rounded-[1px] border-black border block hover:bg-gray-100 transition-colors" {...props}>
    <slot />
  </button>
)}
